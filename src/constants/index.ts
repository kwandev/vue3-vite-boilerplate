export const SECONDS: number = 1000;
export const MINUTES: number = SECONDS * 60;
export const HOURS: number = MINUTES * 60;
export const HALF_HOURS: number = HOURS * 0.5;

export const ACCESS_TOKEN: string = 'ACCESS_TOKEN';
