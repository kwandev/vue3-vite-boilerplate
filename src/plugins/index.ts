const plugins = {
  install: (app: any, options: any) => {
    // app.use();
    // app.component('');
    // app.mixin({
    //   created() {},
    //   beforeUnmount() {},
    // });
  },
};

export default plugins;
